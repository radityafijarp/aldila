# Your solution here
# Load the mtcars dataset
# data(mtcars)
latihan_mtcars = data(mtcars)
latihan_mtcars
# Display the first few rows of the dataset
head(mtcars)

# print(mtcars[["hp"]])
column='hp'
filter_hp=mtcars[[column]]>100
filter_hp

filtered_hp_data=mtcars[filter_hp, ]

filtered_hp_data_50=mtcars[mtcars$hp>50,]

filtered_data <- mtcars[mtcars$mpg > 20, ]

mtcars$mpg > 20


mobil_kencang=mtcars[mtcars$hp>175,]


columns = c("mpg","hp","disp")
for (x in columns){
  print(x)
  print(mtcars[x])
}









